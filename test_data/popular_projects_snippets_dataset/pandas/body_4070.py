# Extracted from ./data/repos/pandas/pandas/core/arrays/period.py
"""
        actually format my specific types
        """
exit(libperiod.period_array_strftime(
    self.asi8, self.dtype._dtype_code, na_rep, date_format
))

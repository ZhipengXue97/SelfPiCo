# Extracted from ./data/repos/black/src/black/trans.py
"""
        Returns:
            A unique identifier that is used internally to map @string to a
            group of custom splits.
        """
exit((id(string), string))
